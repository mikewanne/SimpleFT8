[deepseek] ~34797 Tokens, 4 File(s) → deepseek-v4-pro
[deepseek] V4-Pro denkt — kann 6-30s dauern ...
[deepseek] in=39252 out=10591 total=49843
| Severity | Finding | Stelle im Prompt | Empfehlung |
|----------|---------|-----------------|------------|
| Bug (rot) | Zyklenanzahl widersprüchlich: Prompt sagt „18 Zyklen Rohdaten“, angehängter Code nutzt 8 Zyklen (Schedule-Länge 8). Missverständnis über Messdauer/Struktur. | Zeile 1 (Ziel), Abschnitt 1 | Prompt oder Code vereinheitlichen – entweder 18 Zyklen designen oder Beschreibung auf 8 Zyklen korrigieren. |
| Risiko (orange) | Adaptive-Stop OR stoppt früh basierend auf nur einem Scoring-Modus. Dadurch kann die Auswertung des anderen Modus mit zu wenigen Daten erfolgen und suboptimale Gain-Wahl liefern. | AC8, Z.270-318 | Entweder explizit dokumentieren, dass das Risiko akzeptiert wird (wie geschehen), oder Mindest-Daten-Schwelle für beide Modi überwachen (erhöht Komplexität). |
| Risiko (orange) | `_gain_scoring_mode` wird nach Sub‑Toggle (Std↔DX) nicht aktualisiert. AC6 sagt, es bleibe für UI‑Anzeige; bei Mode‑Wechsel ohne Neumessung zeigt es dann veralteten Modus an. | AC6, mw_radio `_activate_diversity_with_scoring` (nicht explizit im Prompt, aber implizit) | Nach jedem Aufruf von `_activate_diversity_with_scoring` das Flag `_gain_scoring_mode` auf den neuen scoring‑Wert setzen, damit UI konsistent bleibt. |
| Risiko (orange) | AC7‑Fallback `r.get("standard", r)` führt bei altem (nicht‑P51) Dialog dazu, dass Standard‑ und DX‑Store mit identischen Werten beschrieben werden. Später wird das als gültige DX‑Kalibrierung behandelt, obwohl nur eine Messung stattfand. | AC7, mw_radio `_on_dx_tune_accepted` | Zusätzlich prüfen, ob der Dialog überhaupt beide Sub‑Dicts liefert; sonst DX‑Store nicht beschreiben, um falsche Validität zu vermeiden. |
| Risiko (orange) | Unklar, ob `_on_dx_tune_accepted` den Normal‑Modus‑Pfad (nur `normal_presets`) beibehält. AC2 verlangt Schreiben beider Stores unabhängig vom `rx_mode`. Falls der Normal‑Pfad weiter existiert, könnte es zu doppelter Speicherung oder inkonsistentem Verhalten kommen. | AC2, `_on_dx_tune_accepted` im Prompt | Explizit festlegen, ob die Zwei‑Store‑Schreibweise immer gilt (auch bei normaler Kalibrierung) und den Normal‑Pfad entsprechend anpassen oder entfernen. |
| Verbesserung (gelb) | `settings.save_dx_preset` wird zweimal aufgerufen (AC3), obwohl die Stores bereits alle Werte persistieren. Das ist redundant und erhöht Schreib‑IO (Overengineering). | AC3, `mw_radio.py` Z.1458 | Aufruf von `save_dx_preset` weglassen oder auf Legacy‑Modus beschränken, wenn PresetStore die primäre Quelle ist. |
| Verbesserung (gelb) | Schritt‑Label während der Messung soll „Messung läuft (gilt für beide Modi)“ anzeigen. Der aktuelle Code zeigt detaillierte Schritt‑Infos (Runde, Antenne, Gain). Ersatz entfernt nützliche Information. | AC4, `step_label` | Detail‑Info als zweite Zeile erhalten oder `detail_label` nutzen; statischen Text ergänzen statt ersetzen. |
| Hinweis (grau) | Die `_results`‑Struktur dupliziert Werte in Sub‑Keys und Top‑Level. Das erzeugt einen Pflegeaufwand für Synchronisation, auch wenn Backwards‑Compat gewünscht ist. | AC1, `_finish` | Dokumentieren, dass Top‑Level nur ein Spiegel ist, und evtl. eine Property einführen, die dynamisch den aktuellen Sub‑Key liest, statt zu kopieren. |
| Hinweis (grau) | Keine Angabe, ob `_update_results_display` die neuen Marker auch schon während der Messung anzeigen soll (vorläufig). Momentan nur am Ende. Der Prompt spezifiziert nur End‑Anzeige, was passt – könnte aber für Testbarkeit relevant sein. | AC4, `_update_results_display` | In Test‑Spezifikation aufnehmen, ob Zwischen‑Marker getestet werden müssen. |
