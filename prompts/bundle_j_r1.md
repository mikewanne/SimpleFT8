[deepseek] ~31374 Tokens, 5 File(s) → deepseek-v4-pro
[deepseek] V4-Pro denkt — kann 6-30s dauern ...
[deepseek] in=36177 out=10191 total=46368
| Schwere               | Finding                                                                                                                                                                                                                                               | Datei:Zeile                                                          | Empfehlung                                                                                                                                                                                                                            |
|-----------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Risiko (orange)        | Unspezifische Implementierung des `app_version`-Parameters: Prompt sagt nur, dass `ConnectStatusDialog` einen Konstruktor-Parameter `app_version` bekommen soll, aber nicht, dass dieser in einer Instanzvariable gespeichert und in `_setup_ui` für das Footer-Label genutzt werden muss. Fehlt diese Logik, wird der Footer keinen Text anzeigen. | Bundle J V2, AC1, `connect_status_dialog.py:__init__` und `_setup_ui` | Explizit: `__init__(self, parent, app_version="")` → `self._app_version = app_version`; in `_setup_ui` Footer-Label mit `f"SimpleFT8 v{self._app_version} · MIT License"` füllen.                                                       |
| Verbesserung (gelb)    | Overengineering: **Punkt 2 – Einheitlicher Help-Dialog** zwingt alle `?`-Hints (auch kurze wie „Dein Amateurfunk-Rufzeichen...“) in den großen 700×600‑Dialog mit Weißraum. Das erhöht die Code-Komplexität (neue Datei, Klasse, Importe) ohne echten Mehrwert. KISS‑Prinzip verletzt: QMessageBox war für Kurzinfos ausreichend, nur der lange Bandpilot‑Help brauchte Scrollbar/Resize. | Bundle J V2, Punkt 2, AC2                                          | `show_simple_help` nur für `_show_bandpilot_help` verwenden; die anderen `?`-Buttons weiterhin mit `QMessageBox.information` belassen. Alternativ die Größe des Help‑Dialogs dynamisch an den Inhalt anpassen (z. B. per `sizeHint`), um Weißraum zu vermeiden. |
| Verbesserung (gelb)    | Fehlende detaillierte Stylesheet-Spezifikation für `SimpleHelpDialog`: Prompt nennt nur „Hintergrund #1a1a2e, Text #CCC, Border #333 (gleiches Theme wie SettingsDialog)“. Es fehlen Angaben zu `QTextBrowser`, `QPushButton`, Scrollbar, Schriftart etc. – kann zu Inkonsistenzen mit dem restlichen App-Theme führen. | AC2, `ui/simple_help_dialog.py`                                     | Vollständiges CSS-Stylesheet angeben, das mindestens `QDialog`, `QTextBrowser`, `QPushButton` (Schließen‑Button) und `QScrollBar` konsistent mit `SettingsDialog` gestaltet (z. B. `background-color: #1a1a2e; color: #CCC; ...`).       |
| Hinweis (grau)         | Footer-Alignment kann doppelt angegeben werden: Prompt sagt `QLabel.setAlignment(Qt.AlignmentFlag.AlignRight)` und `addWidget(…, alignment=Qt.AlignmentFlag.AlignRight)`. Beides gleichzeitig ist zwar nicht falsch, aber möglicherweise verwirrend. | AC1, `connect_status_dialog.py:_setup_ui`                            | Klarstellung: `QLabel`‑Inhalt standardmäßig linksbündig lassen und nur über das Layout‑Alignment die rechte Position erzwingen. Oder explizit dokumentieren, dass beides verwendet wird.                                                |
| Verbesserung (gelb)    | Behandlung von `delta_db == 0`: Wenn das Delta exakt 0 dB ist, würde das neue Label `(RX: ANT2 ↑0.0 dB)` anzeigen – technisch korrekt, aber verwirrend (es gibt keinen Empfangsgewinn). Der Prompt spezifiziert nur „mit delta“ (wenn `delta_db is not None`), nicht den Sonderfall 0. | AC3, `ui/mw_qso.py:105-109`                                          | Bei `delta_db` < 0.05 dB (oder == 0) das Label ohne Pfeil/Wert als `(RX: ANT2)` anzeigen, um Irreführung zu vermeiden.                                                                                                                |
| Risiko (orange)        | Unklare Testbarkeit von T4/T5: `_make_info_btn` und `_show_bandpilot_help` müssen nach dem Import von `show_simple_help` den neuen Aufruf verwenden. `_make_info_btn` ist eine Funktion auf Modulebene in `settings_dialog.py` – der Import muss auf Modulebene erfolgen, damit die Mock‑Pfade (`ui.settings_dialog.show_simple_help`) funktionieren. Prompt sagt das zwar („Module-Top-Import“), aber es könnte übersehen werden, dass `_make_info_btn` **vor** der Klasse definiert ist und der Import dann für sie sichtbar ist. | Bundle J V2, Abschnitt 6, T4, T5, `settings_dialog.py:10`           | Explizit: `from ui.simple_help_dialog import show_simple_help` ganz oben in `settings_dialog.py` einfügen, und innerhalb von `_make_info_btn` den Lambda-Callback auf `lambda: show_simple_help(btn.window(), "Info", hint)` ändern. |
| Verbesserung (gelb)    | SR6 / AC1: `setFixedSize(352, 196)` – die feste Höhe könnte bei anderen Plattformen/Schriftgrößen den Footer abschneiden. Besser wäre `setMinimumHeight` oder dynamischere Größenanpassung, aber KISS erlaubt das. Kein kritisches Problem, nur Hinweis auf zukünftige Wartbarkeit. | AC1, `connect_status_dialog.py`                                      | Optional: Footer-Höhe über `sizeHint` ermitteln und `setFixedSize` dynamisch anpassen, um plattformübergreifende Robustheit zu erhöhen.                                                                                                |

Hinweis: Die Schweregrade folgen den Vorgaben – kein Bug (rot) gefunden, der unmittelbar zu einem fehlerhaften Programmverhalten durch den Prompt allein führen würde. Das Overengineering‑Finding ist bewusst als Verbesserung (gelb) eingestuft, da es explizit als Fehler benannt werden soll und der Prompt die KISS‑Präferenz betont.
