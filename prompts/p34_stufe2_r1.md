[deepseek] ~107462 Tokens, 10 File(s) → deepseek-reasoner
[deepseek] R1 denkt — kann 6-30s dauern ...
[deepseek] in=121581 out=5735 total=127316
**Prompt-Kritik: P34-Stufe2 V2**

| Schwere | Finding | Datei:Zeile (Referenz im Prompt) | Empfehlung |
|---------|--------|---------------------------------|------------|
| **🔴 Bug** | **Radio.ip=None + `_enable_diversity` ruft implizit `_dynamic_ctrl.activate()` auf** – Bei deferred‑Init (P35 Bug A) wird `_enable_diversity` nicht vollständig ausgeführt (nur `_pending_diversity_init` gesetzt). Der Prompt sagt in AK7 „Deferred‑Init bleibt“, definiert aber nicht, dass `activate()` **nicht** aufgerufen werden darf. Im aktuellen Code (mw_radio.py Zeilen ~469–480) wird bei `radio.ip=None` direkt zurückgekehrt, ohne `activate()`. Fehlt dieser Guard in der neuen `_enable_diversity`, crasht die App beim Start ohne Radio. | AK7, AK4, Abschnitt „Deferred-Init bei radio.ip=None…“ | **Bedingung in `_enable_diversity` muss explizit erhalten bleiben:** Bei `radio.ip=None` nur `_pending_diversity_init` setzen, **kein** `_dynamic_ctrl.activate()` aufrufen. |
| **🟠 Risiko** | **AK11/AK5: `_operate_cycles`-Ersatz und Pattern‑Wahl** – Der Prompt öffnet drei Optionen (A/B/C) und behauptet, V2 tendiere zu C (`choose(slot_index)`). Aber AK5 entfernt bereits `_operate_cycles` und AK11 sagt „R1‑Klarstellung erforderlich“. Ohne finalisierte Signatur kann der Code nicht geschrieben werden – der Prompt liefert hier eine offene Frage statt einer verbindlichen Vorgabe. | AK11, Q2 | **Verbindliche Entscheidung in V3 treffen:** `choose(slot_index)` (Option C) ist KISS und passt zu „mw_cycle hat slot_index ohnehin“. Setze dies als Default, nicht als Tendenz. |
| **🟠 Risiko** | **Cache‑Ratio‑Start bei Pending‑Resume (AK7a)** – Der Prompt definiert V2‑Default „Start IMMER 50:50“ und delegiert die endgültige Entscheidung an R1. Dies erzeugt eine Abhängigkeit: Wird R1 anders entscheiden, müssen AK7 und Implementierung nachgearbeitet werden. Für einen finalen Prompt ist das zu weich. | AK7a, Q1 | **Entscheide bereits in V2 mit Begründung:** Entweder „50:50, weil Dynamic in <75 s übernimmt“ oder „Cache‑Ratio aus Preset, weil Mike sofortiges altes Verhältnis sehen will“. Kein „R1 entscheidet“. |
| **🟡 Verbesserung** | **Commit‑Granularität** – 15 atomare Commits (C1–C15) sind sehr fein. Beispielsweise könnten C6 (mess_status_dialog löschen), C7 (mw_radio.py Statik entfernen) und C9 (main_window.py Toggle entfernen) in einem Commit zusammengefasst werden, da sie funktional eng gekoppelt sind. Das reduziert Merge‑Konflikte und Review‑Aufwand. | Abschnitt 3 (Implementierungs‑Plan) | **Optimiere auf 8–10 Commits:** Fasse UI‑Loeschungen (C6, C7, C9) sowie Test‑Löschungen (C1) + Test‑Anpassungen (C12) jeweils zusammen. Halte nur dort atomar, wo unabhängige Risiken bestehen. |
| **🟡 Verbesserung** | **Silent Ignorieren alter ratio‑Felder (EC11)** – Der Prompt sagt „Akzeptabel“. Ein Benutzer, der nach dem Update altes Preset aufruft, sieht weder Fehler noch Hinweis, dass Ratio‑Daten jetzt unbeachtet sind. Das kann zu Verwirrung führen („Warum zeigt mein Antennen‑Panel 50:50 obwohl 70:30 gespeichert war?“). | EC11, AK9 | **Logge einmalig beim Laden jedes alten Presets eine Informationsmeldung**, z.B. „[PresetStore] Ratio-Felder in {Key} ignoriert (neue Version nutzt dynamisches Verhältnis)“. |
| **🔵 Hinweis** | **Testanzahl ungenau** – Der Prompt sagt „1239 → ~1180 grün (–~80 Statik + +~16 P34)“. 1239 – 80 + 16 = 1175, nicht 1180. Die Differenz ist klein, aber inkonsistent. | AK18 letzter Satz | **Berichtige auf 1175** oder gib eine plausible Begründung für +5 zusätzliche Tests (z.B. „+5 Smoke‑Tests für Migration“). |
| **🔵 Hinweis** | **Zeilenangaben aus aktuellem Code** – Der Prompt verweist auf konkrete Zeilen wie „mw_radio.py:683‑688“ oder „mw_cycle.py Z.93‑94“. Da die Dateien durch das Refactoring massiv verändert werden, sind diese Angaben nach C7/C8 obsolet und können irreführen. | Abschnitt 3 (C7), AK3 | **Ersetze Zeilen‑Referenzen durch Funktions‑ oder Variablennamen** (z.B. „Auto‑Reactivate‑Block in `_on_band_changed`“). |
| **🟡 Verbesserung** | **Fehlende Unit‑Tests für `_enable_diversity` ohne Radio** – Der Prompt testet `test_enable_diversity_activates_dynamic` (T5), aber nicht den Fall `radio.ip=None` (deferred‑Init). Gerade dieser Pfad ist krisch für P35 Bug A und sollte abgedeckt sein. | AK18 (Tabelle), P35 Bug A | **Füge neuen Test hinzu:** `test_enable_diversity_deferred_no_radio` – prüft, dass bei fehlender Radio‑Verbindung `_enable_diversity` keinen `activate()`‑Aufruf macht und `_pending_diversity_init` korrekt setzt. |

**Zusammenfassung:** Der Prompt ist detailliert und gut strukturiert, leidet aber unter drei ungelösten Abhängigkeiten (AK11, AK7a, AK4/Radio.ip). Diese müssen vor V3 verbindlich spezifiziert werden, sonst entsteht Nacharbeit. Die Commit‑Aufteilung ist zu granular – KISS empfiehlt hier gröbere Brocken. Zudem fehlen Tests für den wichtigen deferred‑Init‑Pfad. Die Hardware‑Schutz‑Anweisungen sind korrekt.
