## Kritik am Review-Prompt: Lücken, Unklarheiten, Widersprüche und Verbesserungen

| Schwere | Finding | Datei:Zeile (im Prompt) | Empfehlung |
|---------|---------|------------------------|------------|
| **Bug (rot)** | `advance()` wird **vor** `should_tx()` aufgerufen – `slot_index` ist bereits erhöht, sodass die TX-Entscheidung den **falschen Slot** betrifft (nächsten statt aktuellen). Der existierende Code ruft `should_tx()` vor `advance()` auf. Die geplante Änderung kehrt diese Reihenfolge um, ohne eine Begründung oder Kompensation. | `ui/mw_cycle.py` Z.592+: "Nach `advance()` (Z.592) neuer Block: `should_tx()`..." | Zwingend Reihenfolge korrigieren: `should_tx()` **vor** `advance()` aufrufen und erst nach der TX-Entscheidung den Slot weiterschalten. Alternativ in der Änderung explizit dokumentieren, dass `advance()` in einer separaten Phase aufgerufen wird (z.B. nach TX-Start). |
| **Bug (rot)** | **Scope-Widerspruch**: Akzeptanzkriterium 7 ("Block-Wechsel nach 80 Zyklen korrekt") testet **die Block-Wechsel-Logik**, die im Prompt explizit als "Nicht im Scope" deklariert ist ("Block-Wechsel-Logik in omni_tx.py — unverändert"). Das Kriterium ist nicht erfüllbar, wenn die Logik nicht verändert werden darf. | Akzeptanzkriterien → Punkt 7 vs. "Nicht im Scope" | Akzeptanzkriterium 7 entfernen oder in den Scope aufnehmen. |
| **Risiko (orange)** | **Root Cause wird nicht behoben, sondern nur umgangen**: Das eigentliche Problem ist, dass `_send_cq()` den State **vor** dem Senden auf `CQ_CALLING` setzt, wodurch `on_cycle_end()` blockiert wird. Die Änderung führt ein Flag `auto_cq_enabled` ein, das den Auto-Trigger deaktiviert – der Fehler in `_send_cq()` bleibt aber bestehen und kann in anderen Pfaden (z.B. nach Stop/Start) erneut auftreten. | `core/qso_state.py` Root Cause + Geplante Änderung Punkt 1 | Statt eines zusätzlichen Flags sollte `_send_cq()` den State erst nach erfolgreichem `send_message.emit()` (oder nach Bestätigung des Senders) setzen. Das reduziert Komplexität und behebt die Ursache dauerhaft. |
| **Risiko (orange)** | **Fehlende Integration in den Cycle-Ablauf**: Der neue Block in `mw_cycle.py` wird "nach `advance()`" eingefügt, aber der Code zeigt nicht, wo `_in_qso` herkommt oder wie der State `CQ_WAIT` in den RX-Slots erhalten bleibt. Der Prompt lässt offen, ob der Cycle-Decoded-Hook vor oder nach der Diversity-Akkumulation feuert – das könnte zu State-Races führen. | `ui/mw_cycle.py` Beschreibung | Eine Sequenzgrafik oder Pseudocode für einen kompletten Slot-Durchlauf anfügen, aus der hervorgeht: 1) Dekodierung → 2) RX-Verarbeitung → 3) `advance()` → 4) TX-Entscheidung → 5) State-Machine-Aufruf. |
| **Risiko (orange)** | **Doppeltes Setzen von `cq_mode`**: In `omni_drive_cq()` wird `self.cq_mode = True` gesetzt, obwohl OMNI nur aktiv sein kann, wenn bereits CQ läuft (per Akzeptanzkriterien). Das kann zu einem unerwarteten Wechsel des CQ-Modus führen, wenn OMNI nach einem manuellen CQ-Stopp startet. | `core/qso_state.py` `omni_drive_cq()` | Bedingung einbauen: `if not self.cq_mode: self.cq_mode = True`. Oder in der Dokumentation klarstellen, dass OMNI nur bei aktivem CQ-Modus gestartet werden darf (und das vorher sichergestellt wird). |
| **Verbesserung (gelb)** | **Overengineering durch dedizierte Methode `omni_drive_cq()`**: Die Methode kopiert Teile von `start_cq()` (setzt `cq_mode`, ruft `_send_cq()`). Besser: `start_cq()` um einen optionalen Parameter `target_even` erweitern oder eine Helfer-Funktion `_prepare_cq_slot()` einführen, die sowohl von `start_cq()` als auch von OMNI genutzt wird. Reduziert 2 Code-Pfade auf einen. | `core/qso_state.py` Geplante Änderung, Punkt 1 | `start_cq()` um `target_even: bool | None = None` erweitern. Wenn `target_even` gesetzt, wird `auto_cq_enabled=False` gesetzt und die Parität im Encoder gespeichert. Das eliminiert die neue Methode. |
| **Verbesserung (gelb)** | **Fehlender Hinweis auf Teststrategie**: Der Prompt erwähnt "1014 Tests", aber es gibt kein Wort darüber, wie die neuen Code-Pfade getestet werden (Unit-Tests für `omni_drive_cq()`, Integrationstests für den Cycle-Ablauf). Ohne Tests ist die Änderung riskant, zumal der Root Cause bereits auf eine subtile State-Machine-Bug hinweist. | Allgemein | Mindestens 3 Akzeptanztests fordern: 1) OMNI aktiv → state bleibt CQ_WAIT in RX-Slots, 2) Blockwechsel nach 80 Zyklen korrekt, 3) QSO-Resume setzt `auto_cq_enabled=True`. |
| **Hinweis (grau)** | **Unvollständige Dateiauszüge**: Die angehängten Dateien brechen mitten in einer Methode ab (z.B. `core/qso_state.py` endet mitten in `_process_cq_reply`). Dadurch fehlen Kontext zu `_was_cq`, `_caller_queue` etc., die in der Änderung referenziert werden. Der Reviewer kann die Auswirkungen nicht vollständig beurteilen. | Alle angehängten Dateien | Entweder vollständige Dateien anhängen oder die relevanten Zeilenabschnitte so wählen, dass alle referenzierten Symbole im gezeigten Code definiert sind. |
| **Hinweis (grau)** | **`_omni_target_even` wird nicht zurückgesetzt bei Fehlschlag**: In `omni_drive_cq()` wird das Attribut gesetzt, dann `_send_cq()` aufgerufen. Wenn `_send_cq()` wegen `_pending_reply` eine andere Aktion ausführt (siehe Defense-in-Depth), bleibt `_omni_target_even` hängen. Der nächste CQ-Aufruf könnte die falsche Parität verwenden. | `core/qso_state.py` `omni_drive_cq()` | `_omni_target_even` nur setzen, nachdem sichergestellt ist, dass `_send_cq()` auch wirklich einen CQ-Start auslöst (z.B. nach dem `if self._pending_reply...`-Block). |

---

### Zusammenfassung der Kernkritikpunkte

1. **Reihenfolge-Fehler** im Cycle-Hook (Bug) – sofort korrigierbar.  
2. **Scope-Widerspruch** bei Akzeptanzkriterium