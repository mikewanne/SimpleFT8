[deepseek] ~61960 Tokens, 6 File(s) → deepseek-v4-pro
[deepseek] V4-Pro denkt — kann 6-30s dauern ...
[deepseek] in=71064 out=7748 total=78812
| Severity  | Finding | Datei:Zeile | Empfehlung |
|-----------|---------|-------------|--------------|
| **Bug** (rot) | Punkt 4: Die vorgeschlagene Fix-Erweiterung (nur `qso_sm.stop_cq()`) verhindert nicht, dass ein bereits im Encoder‑Thread wartender CQ-Slot („armed‑er Encoder‑Slot“) nach dem Modus‑Wechsel gesendet wird. Die im Prompt selbst beschriebene Race‑Quelle (mw_radio.py:541‑545) wird damit nicht beseitigt. | `ui/mw_radio.py:541‑545` | Ergänze um `encoder.abort()`, sofern `encoder.is_transmitting` und noch kein Audio‑Streaming läuft (Hardware‑Sicherheit beachten). Andernfalls wird der Feld‑Test‑Symptom („ein weiterer CQ‑Slot gesendet“) erneut auftreten. |
| **Risiko** (orange) | Punkt 4 AC4.3: Verweist auf eine „Encoder‑Pending‑Queue“ (`core/encoder.py` P5.OMNI‑PATTERN‑FIX‑3), die im aktuellen Code‑Stand nicht vorhanden ist. Die Anforderung, diese Queue zu leeren, ist nicht umsetzbar und führt zu Verwirrung. | AC4.3 (Bundle I) | Entweder präzisieren, was genau mit „Pending‑Queue“ gemeint ist, oder die Anforderung streichen. Falls keine solche Queue existiert, genügt der Hinweis, dass kein neuer TX‑Slot begonnen werden darf. |
| **Risiko** (orange) | Punkt 4 Test T4.2 testet nur den Fall ohne vorherigen `send_message`‑Emit (kein aktiver Encoder). Ein realistischer Szenario (CQ lief bereits, Encoder wartet) wird nicht abgedeckt – der Fix kann fälschlich als erfolgreich gewertet werden. | Test‑Beschreibung T4.2 | Test erweitern: Zustand `CQ_CALLING` mit bereits emittierten `send_message` und laufendem Encoder, dann Modus‑Wechsel simulieren und prüfen, dass der Encoder‑Thread rechtzeitig abbricht und kein TX erfolgt. |
| **Hinweis** (grau) | Punkt 1 T1.4: Die Überprüfung des Stylesheets nur auf String‑Inhalt (`width: 18px`) garantiert nicht, dass die Checkbox‑Indikatorgröße tatsächlich angewendet wird (Cascading, Repaint). | Test‑Spezifikation T1.4 | Besser die tatsächliche Indikatorgröße per `QStyle.pixelMetric(QStyle.PM_IndicatorWidth)` abfragen oder einen visuellen Größenvergleich durchführen. |
| **Unklarheit** (gelb) | Offene Frage 2 im Prompt (Pfade bei `_apply_normal_mode` ohne `_on_rx_mode_changed`): Ob ein nötiger CQ‑Stop in diesen internen Restore‑Pfaden erforderlich ist, bleibt ohne R1‑Antwort ungeklärt. Diese Unsicherheit kann zu inkonsistentem Verhalten führen. | `ui/mw_radio.py:464,1167`, AC4.6 | Vor Implementierung Klärung durch R1 herbeiführen und ggf. Test für diese Pfade ergänzen, damit nicht versehentlich ein CQ aktiv bleibt. |
