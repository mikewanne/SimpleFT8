# P150 V3 — Decoder-Empfindlichkeit FT8-Only (R1-finalisiert)

## R1-V4-pro Findings eingearbeitet

- **O1 ORANGE — FT4/FT2 NICHT mit ändern.** Costas-Pattern-Längen
  unterschiedlich → Score-Skala nicht 1:1. FT8 zuerst, FT4/FT2 nach Mike's
  FT8-Erfahrungen separat justieren.
- G1/G2/G3 GELB → Backlog, jetzt nicht.

## Code-Änderung (1 Stelle statt 3)

### `ft8_lib/libft8simple.c` Z. 114 (FT8-Pfad)

```c
- const int kMin_score = 10;
+ const int kMin_score = 4;   // P150 (v0.98.32, 27.05.2026): -24 dB DX-Empfindlichkeit (DeepSeek-V4-pro-validiert)
```

**Z. 369 (FT4) und Z. 513 (FT2) bleiben unverändert bei 10.**

### `main.py`

```python
- APP_VERSION = "0.98.31"
+ APP_VERSION = "0.98.32"
```

## Build-Pipeline

```bash
cd ft8_lib

# fft-Objekte (one-time)
mkdir -p .build/fft
cc -O3 -DHAVE_STPCPY -I. -c -o .build/fft/kiss_fft.o  fft/kiss_fft.c
cc -O3 -DHAVE_STPCPY -I. -c -o .build/fft/kiss_fftr.o fft/kiss_fftr.c

# dylib linken
cc -O3 -DHAVE_STPCPY -I. -dynamiclib -o ../libft8simple.dylib \
   libft8simple.c .build/ft8/*.o .build/common/*.o .build/fft/*.o
```

## Tests

1. dylib lädt + Symbole sichtbar
2. Volle pytest-Suite 2171/2171
3. R1-Empfehlung: Audio-Dump-Replay falls verfügbar

## Rollback

`cp Appsicherungen/2026-05-27_v0.98.31_vor_p150_p151/libft8simple.dylib .`

