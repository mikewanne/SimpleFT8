# v0.90 — Mess-Pattern-Bug-Fix (V3, R1-Findings eingearbeitet)

**Status:** V1 → V2 (Self-Review) → R1 (DeepSeek-R1) → **V3 final**.
Bereit fuer Plan + Code.

## R1-Findings (Bilanz)

| # | Finding | Status |
|---|---|---|
| 🔴 | End-to-End-Test fehlt (Median+Ratio nach `record_measurement`) | **eingebaut** AC3-3 |
| 🔴 | Bandwechsel-Race-Hinweis in HANDOFF behalten | **eingebaut** AC5 |
| 🟡 | mw_radio/mw_cycle nach „4 Zyklen"-Stellen pruefen | **gepruegt**: 0 Treffer Code, aber 4 Doku-Treffer (README+docs) |
| 🟡 | Code-Kommentar in `_evaluate()` Statistik-History | **eingebaut** AC2-5 |
| 🟡 | Geschlossene Even+Odd-Paare pro Antenne testen | **eingebaut** AC3-4 |
| 🟡 | Singleton-Asymmetrie quantitativ einschaetzen | **abgelehnt** als overengineering — Singletons 4+5 sind im Median statistisch egal |
| 🟡 | Regressions-Test gegen altes Pattern | **abgelehnt** — Ratio-Test fängt das schon |
| 🟡 | bestehende Tests strenger machen | **abgelehnt** — neuer Ratio-Test deckt das ab |

## Akzeptanzkriterien (final)

### AC1 — Pattern-Fix (`core/diversity.py`)

- **Z.86** → `("A1","A1","A2","A2","A1","A2")[self._measure_step % 6]`
- **Z.85** Inline-Kommentar:
  `# Mess-Pattern: A1,A1,A2,A2,A1,A2 — fair 3:3, Even+Odd-Paar je Antenne`

### AC2 — Doku-Updates `core/diversity.py`

1. **Z.7** Module-Docstring:
   ```
   Auswertung: Median ueber 3 Zyklen pro Antenne (6-Slot fair 3:3),
   Schwelle 8% (statt 15%).
   ```

2. **Z.79-83** `choose()` docstring praezisieren:
   ```
   In der Mess-Phase bekommt jede Antenne mindestens ein zusammenhaengendes
   Even+Odd-Paar (A1: Slots 0-1, A2: Slots 2-3) damit beide Paritaeten
   empfangen werden. Slots 4-5 sind Singles (A1=even, A2=odd) zur
   Aufrundung 3:3.
   ```

3. **Z.403** print:
   `print("[Diversity] Bandwechsel — Neueinmessung gestartet (6 Zyklen)")`

4. **`_evaluate()`** Code-Kommentar (R1-Empfehlung):
   ```
   # Pre-v0.90 Mess-Pattern war 4×A1 + 2×A2 (struktureller Bias zu ANT1).
   # Seit v0.90 fair 3:3 — Median-Robustheit beider Antennen gleich.
   ```
   Direkt nach `m1 = self._measurements["A1"]` (Z.420).

### AC3 — Tests (`tests/test_patterns.py`)

**Test 1 — Ratio fair (V2):**
```python
def test_measure_ratio_balanced():
    """Mess-Pattern: 3xA1 + 3xA2 ueber 6 Slots — fair (v0.90 Bug-Fix)."""
    dc = DiversityController()
    dc._phase = "measure"
    p = get_pattern(dc, 6, mode="measure")
    assert p.count("A1") == 3, f"Erwartet 3xA1: {p}"
    assert p.count("A2") == 3, f"Erwartet 3xA2: {p}"
```

**Test 2 — Loop nahtlos (V2):**
```python
def test_measure_seamless_loop():
    """Mess-Pattern: max 2 hintereinander auch am Loop-Uebergang."""
    dc = DiversityController()
    dc._phase = "measure"
    p = get_pattern(dc, 6, mode="measure")
    check_seamless_loop(p, 2)
```

**Test 3 — End-to-End Auswertung (🔴 R1-KRITISCH, neu in V3):**
```python
def test_measure_evaluate_fair_yields_5050():
    """Gleiche Scores fuer A1 und A2 → Ratio 50:50, kein dominanter Bias."""
    from core.diversity import DiversityController
    dc = DiversityController()
    dc._phase = "measure"
    # 3 fair-Messungen je Antenne → identisch
    for _ in range(3):
        dc.record_measurement("A1", score=0.0, station_count=10)
        dc.record_measurement("A2", score=0.0, station_count=10)
    # Nach 6 Messungen → _evaluate() triggert
    assert dc.phase == "operate"
    assert dc.ratio == "50:50"
    assert dc.dominant is None


def test_measure_evaluate_a1_dominant_yields_7030():
    """A1 deutlich besser → Ratio 70:30, dominant=A1."""
    from core.diversity import DiversityController
    dc = DiversityController()
    dc._phase = "measure"
    for _ in range(3):
        dc.record_measurement("A1", score=0.0, station_count=20)
        dc.record_measurement("A2", score=0.0, station_count=10)
    assert dc.phase == "operate"
    assert dc.ratio == "70:30"
    assert dc.dominant == "A1"
```

**Test 4 — Geschlossene Even+Odd-Paare pro Antenne (🟡 R1-Empfehlung):**
```python
def test_measure_closed_pairs_per_antenna():
    """A1 hat geschlossenes Paar Slots 0-1, A2 hat geschlossenes Paar Slots 2-3."""
    dc = DiversityController()
    dc._phase = "measure"
    p = get_pattern(dc, 6, mode="measure")
    # A1 in 0-1 zusammenhaengend
    assert p[0] == "A1" and p[1] == "A1", f"A1 0-1 Paar fehlt: {p}"
    # A2 in 2-3 zusammenhaengend
    assert p[2] == "A2" and p[3] == "A2", f"A2 2-3 Paar fehlt: {p}"
```

**Bestehende Tests (bleiben gruen):**
- `test_measure_both_antennas`, `test_measure_parities`, `test_measure_max_consecutive`
- `test_diversity_phase_transition_after_6_measurements`
- `test_diversity_measure_even_odd`, `test_diversity_50_50_pattern`,
  `test_diversity_70_30_pattern`
- `test_phase_diff_detects_measure_to_operate_transition`
- `test_reset_phase_is_measure_not_operate`, `test_on_band_change_triggers_full_reset`

### AC4 — `main.py` APP_VERSION
- `APP_VERSION = "0.89"` → `APP_VERSION = "0.90"`

### AC5 — 4-Datei-Update + R1-erweitert

1. **`HISTORY.md`** anhaengen — `## 2026-05-04 v0.90 — Mess-Pattern-Bug-Fix`
   - Workflow-Spalte (V1 → V2 → R1 → V3)
   - R1-KRITISCH-Findings (End-to-End-Test, Bandwechsel-Race-Hinweis)
   - Pattern-Wechsel + Begruendung
   - Statistik-Disclaimer Pre-v0.90
   - Tests 663 (= 659 + 4 neue)
   - Doku-Synchronisation (README DE/EN + docs/explained DE/EN)
   - Field-Test-Erwartung

2. **`HANDOFF.md`** in **beiden** Pfaden (`/FT8/HANDOFF.md` + `/FT8/SimpleFT8/HANDOFF.md`):
   - v0.90-Eintrag oben rein
   - KRITISCH-v0.90-Hinweis raus (erledigt)
   - **🔴 R1: „Offene Verdachts-Punkte" Sektion behalten:**
     ```
     **🟡 Offene R1-Verdachts-Punkte (NICHT in v0.90):**
     - `mw_radio.py` Bandwechsel-Race: laufender Slot wird nach `on_band_change`
       reset noch verarbeitet → record_measurement mit altem Antennen-State auf
       neuem Band moeglich. Verifikation + Token-Pattern-Fix in eigener Iteration
       nach v0.90.
     ```

3. **`CLAUDE.md`** in **beiden** Pfaden:
   - „Aktueller Stand" v0.89 → v0.90 mit Pattern-Fix-Erwaehnung

4. **TODO.md**:
   - 🔴 KRITISCH v0.90 Eintrag entfernen
   - Block 1 Feldtest + Block 2 DRINGEND bleiben
   - Bandwechsel-Race als 🟡 offen ergaenzen

5. **Memory `project_v090_mess_pattern_bug.md`**:
   - Status auf „erledigt 2026-05-04 v0.90" setzen, Kurz-Lessons ergaenzen

6. **`MEMORY.md`** Index entsprechend updaten

7. **R1-Empfehlung Doku-Synchronisation:**
   - `README.md:61` (EN) — „median over 4 cycles" → „median over 3 cycles per antenna (6-slot fair 3:3)"
   - `README.md:581` (DE) — „Median ueber 4 Zyklen" → analog
   - `docs/explained/diversity-modes.md:84-94` (EN) — kompletter Abschnitt
     „How the Measurement Works" auf 6-Slot-Fair-3:3-Pattern updaten
   - `docs/explained/diversity-modes_de.md:82-92` (DE) — analog

### AC6 — Tests-Baseline gruen
- `QT_QPA_PLATFORM=offscreen ./venv/bin/python3 -m pytest tests/ -q`
- Erwartet: **663 passed** (= 659 + 4 neue Tests)
- Bei rot: Stop, Diagnose

## Out-of-Scope (NICHT in v0.90)

- 🔴 Bandwechsel-Race (mw_radio.py): R1 hat zweimal angedeutet, V3 erkennt das
  Risiko explizit, fixiert es als „behalten in HANDOFF" — separater Bug-Fix.
- Block 2 (Adaptiv-Stops + ROUNDS=2): eigener V1→V3-Zyklus.
- `core/diversity 2.py` (untracked Backup vom 30.04.2026): nicht antasten,
  Mike's Sicherungs-Datei, gehoert nicht in Commit.

## Atomare Commits (geplant — final)

1. **`fix(diversity): Mess-Pattern 4:2 → 3:3 fair (v0.90)`**
   - `core/diversity.py` (Z.86 Pattern + Z.85 Kommentar + Z.7/79-83/403/420 docstrings & code-comment)
   - `tests/test_patterns.py` (4 neue Tests)

2. **`docs: Diversity-Doku auf 6-Slot fair 3:3 syncen`**
   - `README.md` (DE+EN: Z.61+581)
   - `docs/explained/diversity-modes.md` (EN)
   - `docs/explained/diversity-modes_de.md` (DE)

3. **`chore: APP_VERSION 0.89 → 0.90 + 4-Datei-Update`**
   - `main.py` APP_VERSION
   - `HISTORY.md` v0.90-Eintrag
   - `HANDOFF.md` (beide Pfade) — v0.90 Stand + Bandwechsel-Race-Hinweis
   - `CLAUDE.md` (beide Pfade) — Aktueller Stand
   - `TODO.md` — KRITISCH raus, Bandwechsel-Race rein
   - Memory `project_v090_mess_pattern_bug.md` (erledigt-Status)
   - `MEMORY.md` Index

## Pipeline-Kosten

- Pattern-Wechsel innerhalb 6-Slot-Window: **0 s Veraenderung**
- MEASURE_CYCLES=6 bleibt
- Pipeline ~4:31 Min unveraendert

## Field-Test-Erwartung

| Metrik | Pre-v0.90 (4:2-Bias) | v0.90 (fair 3:3) |
|---|---|---|
| ANT2-Win-Rate (Mike's 40m) | ~4 % (1/23 Stationen) | erwartet 15-25 % |
| 50:50-Ratio Wahl | selten (Bias zu 70:30 ANT1) | haeufiger bei echter Diff < 8 % |
| Pooled-Mean +88 %/+124 % | valide (konservativ) | erwartet +X % wo X ≥ 88 |

**Statistik-Hinweis:** Pre-v0.90-Daten werden NICHT verworfen — Pooled-Mean
bleibt als historische Bezugsgroesse. Field-Test-Daten ab v0.90 sind die
ersten fairen Messungen.

## Rollback-Anker

- Pre-v0.90: `git checkout aec3706` (letzter Block-1-Commit)
- v0.88.1 (vor Block 1): `git checkout v0.88.1`
